{
  "name": "lingualive-extension",
  "version": "0.2.0",
  "private": true,
  "scripts": {
    "build": "esbuild src/background.ts src/content/hud.ts src/options.ts src/popup.ts --bundle --outdir=dist --sourcemap",
    "build:watch": "npm run build -- --watch"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.258",
    "esbuild": "^0.24.0",
    "typescript": "^5.6.3"
  }
}
